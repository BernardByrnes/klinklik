[tool.pytest.ini_options]
DJANGO_SETTINGS_MODULE = "clinicopus.settings"
pythonpath = ["backend"]
testpaths = ["backend"]
addopts = "-ra"

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "B"]
